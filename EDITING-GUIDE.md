# সাইট এডিট করার সহজ গাইড

এই পোর্টফোলিওতে এখন **দুটো উপায়ে** এডিট করা যায়:

1. **Admin Panel** (নতুন, সহজ) — ব্রাউজারে লগইন করে ফর্ম পূরণ করে এডিট করুন, কোনো কোড ছোঁয়া লাগবে না।
2. **সরাসরি কোড এডিট** (আগের মতো) — `content.json` বা `index.html` ফাইল টেক্সট এডিটরে খুলে বদলান।

---

## ১. Admin Panel ব্যবহার — প্রথমবার সেটআপ

সাইটের সাথে এখন একটা `admin.html` পেজ যোগ করা হয়েছে। এটা GitHub-এর সাথে সরাসরি
কথা বলে, তাই কোনো আলাদা সার্ভার/হোস্টিং লাগে না।

### ধাপ ১: GitHub Personal Access Token বানান
1. GitHub-এ লগইন থাকা অবস্থায় এই লিংকে যান:
   https://github.com/settings/personal-access-tokens/new
2. **Token name**: যেকোনো নাম দিন (যেমন "Portfolio Admin")
3. **Repository access**: "Only select repositories" বেছে নিয়ে আপনার
   `Kazi-Rejaul-Alam-Portfolio` রিপোটা সিলেক্ট করুন
4. **Permissions** → **Repository permissions** → **Contents** → **Read and write** সিলেক্ট করুন
5. নিচে **Generate token** বাটনে ক্লিক করুন
6. যে টোকেনটা দেখাবে (ghp_... দিয়ে শুরু) সেটা **কপি করে নিরাপদ জায়গায় রাখুন**
   (এই পেজ ছেড়ে গেলে আর দেখা যাবে না)

### ধাপ ২: প্রথমবার Admin Panel এ ঢোকা
1. ব্রাউজারে যান: `https://rejaulalam-byte.github.io/Kazi-Rejaul-Alam-Portfolio/admin.html`
2. পাসওয়ার্ড দিন: **`reja2026`** (এটা বদলাতে চাইলে নিচের "পাসওয়ার্ড বদলানো" অংশ দেখুন)
3. এরপর যে টোকেনটা কপি করেছিলেন সেটা পেস্ট করুন, সাথে GitHub username আর repository
   নাম (এগুলো আগে থেকেই পূরণ করা আছে, ঠিক থাকলে বদলাতে হবে না)
4. "সেভ করে এগিয়ে যান" চাপুন — এখন থেকে প্রতিবার শুধু পাসওয়ার্ড দিলেই হবে,
   টোকেন এই ব্রাউজারেই মনে রাখা থাকবে

### ধাপ ৩: এডিট করুন ও পাবলিশ করুন
- প্রতিটা সেকশনের বক্সে গিয়ে টেক্সট বদলান, নতুন এন্ট্রি যোগ/ডিলিট করুন
- ছবি বদলাতে চাইলে "Hero ছবি" বা "About ছবি" প্যানেলে ফাইল বেছে নিন
- সব শেষে উপরের **"Save & Publish"** বাটনে ক্লিক করুন
- ১-২ মিনিট পর লাইভ সাইটে (Ctrl+F5 রিফ্রেশ করে) পরিবর্তনগুলো দেখা যাবে

### পাসওয়ার্ড বদলানো
`admin.html` ফাইলে Ctrl+F দিয়ে খুঁজুন:
```
const ADMIN_PASSWORD = "reja2026";
```
`"reja2026"` এর জায়গায় নিজের পছন্দের পাসওয়ার্ড বসিয়ে ফাইলটা আবার GitHub-এ আপলোড করুন।

### নিরাপত্তা সম্পর্কে সততার সাথে বলে রাখা ভালো
- এই পাসওয়ার্ডটা কোডের ভেতরেই লেখা থাকে, তাই এটা টেকনিক্যালি ১০০% নিরাপদ না —
  কেউ চাইলে পেজ সোর্স দেখে বের করতে পারবে। এটা মূলত সাধারণ দর্শকদের সম্পাদনা
  থেকে আটকানোর একটা বাধা।
- আসল নিরাপত্তা আসে GitHub টোকেন থেকে, যেটা শুধু আপনার নিজের ব্রাউজারে সেভ থাকে,
  কারো কাছে পাঠানো হয় না। টোকেন হারিয়ে গেলে বা ফাঁস হলে GitHub Settings থেকে
  সেটা সাথে সাথে বাতিল (revoke) করে দিতে পারবেন।
- `admin.html` টা প্রকাশ্যে (public) থাকলেও সমস্যা নেই — যতক্ষণ আপনার টোকেন
  আর পাসওয়ার্ড গোপন থাকছে।

---

## ২. সরাসরি কোড এডিট (Admin Panel ছাড়াই)

এই সাইটটা এখন দুটো মূল ফাইলে ভাগ করা:
- **`content.json`** — সব টেক্সট, তারিখ, তালিকা এখানে থাকে (এটাই Admin Panel সম্পাদনা করে)
- **`index.html`** — ডিজাইন/লেআউট/রঙ এখানে থাকে (কমেন্টসহ)

### লেখা (টেক্সট) বদলানো
`content.json` ফাইলটা Notepad বা VS Code দিয়ে খুলুন। এটা একটা সহজ কাঠামো —
`"key": "value"` ফরম্যাটে। শুধু `" "` এর ভেতরের লেখাটুকু বদলান, বাকি চিহ্ন
(`{ } [ ] , :`) স্পর্শ করবেন না।

### রঙ বদলানো
`index.html` ফাইলের একদম উপরের দিকে `:root { ... }` অংশে সব রঙ লেখা আছে,
প্রতিটার পাশে বাংলায় ব্যাখ্যা আছে। শুধু `#FF7A29` এর মতো hex code বদলে দিন।

### ছবি বদলানো
নতুন ছবিটার নাম দিন ঠিক `hero.jpg` বা `profile.jpg` (একই নাম), তারপর `assets`
ফোল্ডারে পুরনোটা রিপ্লেস করে দিন।

---

## পরিবর্তন করার পর (কোড-এডিট পদ্ধতিতে)

1. ফাইলটা সেভ করুন
2. GitHub রিপোতে গিয়ে পুরনো ফাইলটা এডিট বা "Add file → Upload files" দিয়ে
   একই নামে নতুন ফাইল আপলোড করুন
3. "Commit changes" বাটনে ক্লিক করুন
4. ১-২ মিনিট পর সাইট রিফ্রেশ করলে পরিবর্তনটা দেখা যাবে

---

## সাবধানতা
- `content.json` এ কমা (`,`) বা ব্র্যাকেট (`{ } [ ]`) ভুলবশত মুছবেন না —
  একটা চিহ্ন হারালে পুরো ফাইল ভেঙে যেতে পারে। বড় পরিবর্তনের আগে ফাইলের
  একটা কপি আলাদা জায়গায় রেখে দিন।
- `index.html` এ `<` `>` `{` `}` চিহ্নগুলোও একইভাবে সাবধানে রাখবেন।
- কিছু ভুল হলে GitHub-এর "History" (কমিট হিস্ট্রি) থেকে আগের ভার্সনে ফিরে
  যাওয়া সবসময় সম্ভব।

